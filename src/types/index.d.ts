// Shared types (placeholder).

export type GalleryJob = {
  url: string;
};

export type GalleryResult = {
  galleryName: string;
  totalFound: number;
  totalDownloaded: number;
  errors: string[];
};
